# 张文韬工作日志

日期：2026年01月27日

姓名：张文韬

## 一、具体工作任务

1. **架构迁移与持久化**：完成 Electron + SQLite (Better-SQLite3) 的本地数据库集成，实现设备与日志的持久化存储。
2. **UI/UX 深度重构**：移除 UnoCSS，全面迁移至 Tailwind CSS v4 + Shadcn-Vue，实现现代化 Dashboard 界面与无边框窗口。
3. **工程化与 CI/CD**：配置 GitHub Actions 实现多平台自动化构建与发布，修复 Linux/macOS 构建兼容性问题。
4. **缺陷修复**：解决 TitleBar 交互失效、配置项布局错乱、Git 多账号身份冲突等问题。

## 二、任务进展与成果

### 核心功能开发 (Feature)

- **SQLite 数据库集成**：
  - 引入 `better-sqlite3`，在 Electron 主进程实现 SQLite 数据库初始化。
  - 完成 `devices`、`logs`、`config` 表结构设计。
  - 封装 IPC 接口 (`db-add-device`, `db-get-logs` 等)，实现渲染进程对数据库的 CRUD 操作。
  - 优化数据库存储路径为便携式 `data/` 目录，支持绿色版部署。
- **配置管理模块**：
  - 重构配置界面布局，采用 Flex/Grid 混合布局解决“波特率/状态”显示拥挤问题。
  - 增加 IP (IPv4) 正则校验与 AuthID 纯数字校验。
  - 优化批量同步/下发逻辑，拆分 Loading 状态，避免全屏锁定。

### 界面重构与优化 (Refactor)

- **Shadcn-Vue 迁移**：
  - 使用官方 CLI (`pnpm dlx shadcn-vue@latest`) 重新生成标准组件，确保样式原汁原味。
  - 移除 UnoCSS 依赖，解决样式冲突问题。
  - 实现暗色/亮色主题动态切换功能。
- **原生体验增强**：
  - 实现 `frame: false` 无边框窗口。
  - 开发自定义 `TitleBar` 组件，采用“左右分治”布局策略解决拖拽区域遮挡按钮点击的问题。
  - 设计并生成极简风格应用图标，统一 Windows/macOS/Linux 安装包图标。

### 工程化与运维 (DevOps)

- **CI/CD 流水线**：
  - 搭建 GitHub Actions `build.yml`，实现三端并行构建。
  - 解决 Linux 构建缺 `libicns-utils` 和 macOS 文件名中文编码报错问题。
  - 编写 `scripts/release.js` 脚本，实现版本递增、Tag 推送、Release 发布的自动化。
- **环境治理**：
  - 修复 `pnpm` Corepack 签名验证失败问题。
  - 配置 `git config includeIf`，解决多项目多 Git 账号的身份自动切换问题。

###  风险与困难

- **技术难点**：Electron 无边框窗口的 `drag` 区域 (`-webkit-app-region: drag`) 会强行拦截鼠标事件，导致自定义标题栏按钮无法点击。
- **解决方案**：放弃父子嵌套结构，采用 DOM 结构分离方案（左侧拖拽区、右侧按钮区互不重叠），并结合 IPC 日志进行调试。

### 项目运维

- 代码仓库：https://github.com/CherishMvp/longan-ipc-config
- 构建状态：v1.0.6 Release (Draft) 已在 GitHub 生成。

### 附加说明

- 当前版本 v1.0.7 (开发中)
- 待验证：TitleBar 按钮在特定环境下的点击响应情况（已添加 IPC 调试日志）。

## 三、遇到的问题及处理

- **问题 1**：`better-sqlite3` 报错 `Could not locate the bindings file`。
  - **处理**：在 `package.json` 中配置 `postinstall: "electron-builder install-app-deps"`，确保原生模块针对 Electron ABI 重新编译。
- **问题 2**：GitHub Actions 构建 macOS 版本上传失败。
  - **处理**：修改 `artifactName` 配置，强制使用纯英文文件名 (`sensor-config-tools-...`)，避免 URL 编码错误。
- **问题 3**：Git 提交记录作者信息错误。
  - **处理**：通过 `.gitconfig` 的 `includeIf` 指令，实现指定目录加载特定 Git 身份配置。

## 四、未完成工作及原因

- **系统托盘 (System Tray)**：属于 Roadmap 第二阶段规划，尚未开始。
- **局域网设备自动发现**：属于 Roadmap 第二阶段核心功能，待排期。

## 五、会议相关内容

- 无
